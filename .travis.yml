# deploys into AWS infra - with cloudformation template - uses api gteway and lambda
language: node_js

node_js:
  - "5.11"

script:
  npm run test

before_deploy:
  npm run prepdeploy

deploy:
  npm run build

notifications:
    email:
        on_failure: always
