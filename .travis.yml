# deploys into AWS infra - with cloudformation template - uses api gteway and lambda
language: node_js

node_js:
  - "5.11"

script:
  npm run test

before_deploy:
  npm run prepdeploy

deploy:
  - provider: script
    script: npm run deploy
    skip_cleanup: true

notifications:
    email:
        on_failure: always
